{
  "name": "pine-ai-app",
  "private": true,
  "scripts": {
    "install": "echo skipping root install",
    "build": "./build-all.sh",
    "start": "node dist-server/server.js"
  },
  "devDependencies": {
    "concurrently": "^8.0.0",
    "ts-node": "^10.0.0",
    "typescript": "^5.0.0"
  }
}
